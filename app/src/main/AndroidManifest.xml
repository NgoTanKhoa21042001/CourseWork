<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.asm21">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Asm21"
        tools:targetApi="31">
        <activity
            android:name=".Upload"
            android:label="Upload to Cloud Service"
            android:exported="false" />
        <activity
            android:name=".Search"
            android:label="Search"
            android:exported="false" />
        <activity
            android:name=".AddActivity1"
            android:exported="false"
            android:label="Add Expenses"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Expensive"
            android:exported="false"
            android:label="Expense Detail" />
        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:label="Edit Trip"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddActivity"
            android:exported="false"
            android:label="Home"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="All Trip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>